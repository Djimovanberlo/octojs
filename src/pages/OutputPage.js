import React from "react";

import SingleOutput from "../components/SingleOutput";

const OutputPage = () => {
  return (
    <>
      {/* {formData.map((graphInfo) => {
      return <SingleOutput {formData props} />
  })} */}
    </>
  );
};

export default OutputPage;
